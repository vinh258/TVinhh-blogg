# ============================================================
# FILE MẪU - Copy file này thành ".env" (bỏ đuôi .example) rồi điền
# chuỗi kết nối MongoDB Atlas thật của bạn vào.
#
# LƯU Ý: File ".env" thật KHÔNG được đẩy lên GitHub (đã chặn trong .gitignore)
# ============================================================

MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/blog_education_dev?retryWrites=true&w=majority
